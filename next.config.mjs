/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["loremflickr.com", "flagcdn.com", "www.mega.pk", "images.dwncdn.net", "images.pexels.com"],
  },
};

export default nextConfig;
